export type Pi = {
    title: `SL${number}.${number}`;
    sprintTimestamp: {
        first: number;
        second: number;
        third: number;
        fourth: number;
        fifth: number;
        ip: number;
    }
}